# configs/config.yaml
server:
  port: 8080
  jwt_secret: "your_very_secret_key_for_jwt_signing"
  jwt_expiration_hours: 24

database:
  host: localhost
  port: 3306
  user: admin
  password: admin123
  dbname: identity_db